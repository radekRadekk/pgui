import { blue } from "material-ui/colors";

export const lightTheme = {
    palette: {
      type: "light",
      primary: {
        main: blue[200],
      },
      background: {
        default: "#bdbdbd"
      },
    }
  };
  
  export const darkTheme = {
    palette: {
      type: "dark",
      primary: {
        main: blue[800],
      },
    }
  };